openapi: 3.0.3
info:
  title: axiom-e2e-httpbin
  version: "1.0"
servers:
  - url: http://127.0.0.1:18080
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
paths:
  /anything/{id}:
    get:
      security:
        - ApiKeyAuth: []
      summary: path param echo
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /post:
    post:
      summary: json post echo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: OK
